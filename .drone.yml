kind: pipeline
type: docker
name: amd64

platform:
  os: linux
  arch: amd64
steps:

- name: Buildx
  image: docker/buildx-bin
  volumes:
    - name: docker socket
      path: /var/run/docker.sock
  
  commands:
  
  - docker buildx ls
