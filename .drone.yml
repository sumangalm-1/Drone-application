kind: pipeline
type: docker
name: amd64

steps:

- name: Buildx
  image: docker:20
  privileged: true
  settings:
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
  
  
  environment:
    GOOS: linux
    GOARCH: ppc64le
    DOCKER_CLI_EXPERIMENTAL: enabled
  
 
  volumes:
    - name: docker socket
      path: /var/run/docker.sock

  commands:
  - docker buildx version
  - docker buildx create --driver docker-container --use
  - docker buildx build --platform linux/arm64,linux/arm/v8,linux/ppc64le -t test -f Dockerfile .
  - docker tag test  sbm13/multiarch-test:linux/ppc64le
  - docker images
 
  - docker login -u sbm13 -p Sbm@1396
  - docker push sbm13/multiarch-test:linux/ppc64le
  
  - docker buildx ls
  
volumes:
- name: docker socket
  host:
    path: /var/run/docker.sock

